<section class="container container-fluid mt-5">
  <div class="row mt-4">
    <div class="col-md-12 blogpost-list">
      <h3>Posts:</h3>
      {{#if blogposts.length}}
        {{#each blogposts as |blogpost|}}
          <div class="row mb-2 blogpost-list-card">
            <div class="col-md-10">
              <h4><a href="/blogpost/{{blogpost.id}}">{{blogpost.blog_title}}</a></h4>
            </div>
            <div class="col-md-2">
              <button class="btn btn-sm btn-danger" data-id="{{blogpost.id}}">DELETE</button>
            </div>



          <div class="col-md-2">
          <button class="btn btn-sm btn-primary" data-id="{{blogpost.id}}">UPDATE</button>  
          </div>




          </div>
        {{/each}}
      {{else}}
        <p>You have none! Click below to create a post.</p>
      {{/if}}
    </div>
  </div>
  <div class="form-container hidden">
    <h3>Create a New Post:</h3>
    <form class="form new-blogpost-form">
      <div class="form-group">
        <label for="blog_title">Title:</label>
        <input class="form-input" type="text" id="blog_title" name="blog_title" />
      </div>
    
      <div class="form-group">
       <label for="blog_post">Content:</label>
        <textarea rows="4" class="form-input" id="blog_post" name="blog_post"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Create</button>
        <button type="button" id="cancel-form" class="btn btn-secondary">Cancel</button>
      </div>
    </form>
  </div>

  <input type="button" value="+ New Blogpost"  class=" btn btn-info btn-new-blogpost" id="newBlogpostBtn" />


</section>

<script src="./js/profile.js"></script>
